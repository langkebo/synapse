# ============================================================================
# Synapse Rust 环境变量配置
# ============================================================================
# 复制此文件为 .env 并修改配置
# cp .env.example .env
# ============================================================================

# ============================================================================
# 服务器配置
# ============================================================================
# 服务器域名 (必须修改为你的域名)
SERVER_NAME=cjystx.top

# 管理员联系邮箱
ADMIN_EMAIL=admin@cjystx.top

# ============================================================================
# 数据库配置
# ============================================================================
# PostgreSQL 密码 (生产环境必须修改!)
DB_PASSWORD=synapse_secure_password_change_me

# ============================================================================
# 安全密钥 (生产环境必须修改!)
# ============================================================================
# 生成命令: openssl rand -hex 32
SECRET_KEY=change_this_to_random_64_char_hex_string

# 注册共享密钥 (用于创建账户)
# 生成命令: openssl rand -hex 16
REGISTRATION_SECRET=change_this_registration_secret

# 管理员注册密钥
# 生成命令: openssl rand -hex 16
ADMIN_SECRET=change_this_admin_secret

# ============================================================================
# 联邦签名密钥
# ============================================================================
# 使用 ./target/release/generate_test_keypair 生成
# 或使用以下测试密钥 (仅用于开发环境!)
SIGNING_KEY=5XVPWT8O/DyaXT17qJpUnEO5aRl1Cmevojx0+8uPYV8=
KEY_ID=ed25519:testkb1OUw

# ============================================================================
# 日志配置
# ============================================================================
# 日志级别: error, warn, info, debug, trace
RUST_LOG=warn

# 是否显示堆栈跟踪 (生产环境建议关闭)
RUST_BACKTRACE=0

# ============================================================================
# 注册设置
# ============================================================================
# 是否允许公开注册 (生产环境建议关闭)
ENABLE_REGISTRATION=false
